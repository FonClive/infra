user www-data;
worker_processes auto;
pid /run/nginx.pid;

events {
    worker_connections 768;
}

http {
    server {
        listen 80;
        server_name {{ inventory_hostname }};
        location / {
            root /var/www/html;
            index index.html;
        }
    }
}
